# Matchfile for certificate and provisioning profile management

git_url("https://github.com/your-org/remi-mobile-certificates")

storage_mode("git")

type("development") # The default type, can be: appstore, adhoc, development, enterprise

app_identifier(["com.remi.mobile"])
username("developer@remi.com") # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match